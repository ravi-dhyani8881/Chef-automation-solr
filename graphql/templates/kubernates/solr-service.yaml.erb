apiVersion: v1
kind: Service
metadata:
  name: solr-service
  namespace: febe
  labels:
    app: solr
spec:
  selector:
    app: solr
  ports:
    - port: 80
      targetPort: 8983
      protocol: TCP
  type: ClusterIP
