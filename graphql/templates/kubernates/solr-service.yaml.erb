apiVersion: v1
kind: Service
metadata:
  name: solr-service-<%= @ID %>
  namespace: <%= @organizationID %>
  labels:
    app: solr-<%= @ID %>
spec:
  selector:
    app: solr-<%= @ID %>
  ports:
    - port: 80
      targetPort: 8983
      protocol: TCP
  type: ClusterIP
