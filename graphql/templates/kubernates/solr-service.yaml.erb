apiVersion: v1
kind: Service
metadata:
  name: solr-service-<%= @graph_id %>
  namespace: <%= @organizationID %>
  labels:
    app: solr-<%= @graph_id %>
spec:
  selector:
    app: solr-<%= @graph_id %>
  ports:
    - port: 80
      targetPort: 8983
      protocol: TCP
  type: ClusterIP
